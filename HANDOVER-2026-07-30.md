# IOS-IM 工程交接 — 续

更新时间：2026-07-30
工程：`/Users/cofe/IOS-IM/apps/mobile`
分支：`codex/flutter-bootstrap`（本轮工作分支，未合并）

本文件承接 `~/Desktop/IOS-IM_工程交接_2026-07-29.md`。上一轮停在 Task 4 未提交、未验证。本轮把它做完、提交，并继续做真实对话、群聊接线和一批"界面在骗人"的修复。

## 本轮提交（base `2da2d64`，12 个提交）

```
309a7f6 feat: make the provider connection test work
719fe56 fix: resolve group turn models exactly like single chat
733cc9c feat: reply with a natural answer instead of an internal projection
af5d8dd fix: stop the expert hero from inventing a model
ac32bc9 fix: believe a provider's declared model endpoint types
f041867 fix: keep the reason a kernel bootstrap failed
512f902 fix: name the catalog gap instead of blaming storage
9fccf75 feat: run group chat on the production orchestration kernel
1da0b89 feat: add provider-backed agent runtime and billing fence
22f96e8 fix: stop claiming state the app does not have
37188a7 feat: refine chat and expert profile interactions
c4407e0 feat: select global and expert models
```

验证：全量 `flutter test --concurrency=1` **904 passed**；`flutter analyze` 无问题；
`flutter build ios --debug --simulator` 通过；真机（iPhone 17 Pro 模拟器
`4569A31A-A611-4ABE-901B-A3B95A330128`）用真实 DeepSeek Key 实测单聊自然中文回答。

## 真机验证到什么程度

- **单聊真实调用 ✅**：用用户的 DeepSeek Key 实测，发新消息返回自然中文回答，
  「未核验」标识保留，模型标签显示真实绑定（`DeepSeek / deepseek-v4-flash`）。
- **群聊真实调用**：接通了生产编排 kernel，成员自动选择、事件流、UI 都工作；
  修掉了一个"每轮失败"的模型解析 bug（见下）。端到端自然回答尚未在真机上最终确认，
  因为 `flutter test integration_test` 每次重装会清掉容器里的 Key 和模型配置——
  该 bug 的修复由真实 SQLite 单元测试覆盖。
  **下一步**：在持久安装里重配 Key + 模型后手动验一轮群聊。

## 关键修复说明

1. **c4407e0 Task 4**：全局默认 + 专家独立模型，共享串行 mutation 边界，`fitness-planner`
   真专家包，`ModelRoutingController` 加固（rollback 失败不再谎称"原配置仍有效"、
   raw error 包成 `ModelRoutingException`、`close()` drain 读操作、read 带 mutation epoch）。
2. **提交前独立复审挡下两个 P0**：(a) 两条会话改了绑定专家而历史库 schema v1 无迁移路径
   → 加显式 `supersededExpertBindings` 迁移，否则任何旧安装永久开不了 app；
   (b) 恢复了被草稿删掉的"只有 structural profile 可执行"构造期护栏。
3. **移除三个不可执行的联系人**：`legal-risk-advisor` / `fact-checker` / `industry-researcher`
   是 trustedEvidence policy，`validateAndProject` 对非 structural 永远返回 null，
   会让合同审阅/信息监控/深度研究每条回复都被过滤。已从 `_singleChatIds` 和
   installed identities 移除，联系人可见但发消息禁用。**见"待你决定"。**
4. **719fe56 群聊模型解析 bug**：`LiveRoutingAgentRuntime` 曾强制要求全局默认非空，
   而单聊按 `override ?? global` 解析。已改成一致，用真实 SQLite 单元测试钉死。
5. **733cc9c 中文自然回答**：新增 `OutputValueType.answerText` 和保留键 `Answer`，
   气泡直接展示模型的自然中文回答。**安全性靠结构不靠词表**：子代理分析证明词表式散文
   安全闸不可靠（中文完成态无强制形态、否定/条件豁免给攻击者前缀），所以信封仍钉死
   `claimType=advice / verified=false`，UI 始终显示「未核验」，执行类信封直接拒绝。
6. **一批"界面在骗人"的修复**：发送失败误报为可重试（实为未配置模型）→ 改成
   「去配置模型服务」；聊天头部/专家 hero 的死字符串模型标签 → 真实绑定；自建 Gateway 页
   谎称令牌存 Keychain（实际不存）→ 如实说明；本地数据页假存储数字 + 空回调 → 真实读磁盘
   + 明确标注未开放；会话列表点不开的联系人误报"存储不可用" → 改成"该专家未安装"。
7. **309a7f6 连接测试**：只读打一次 `/models`，报「连接正常/Key 无效/额度不足/连接失败」，
   不碰持久化、不进 mutation FIFO。

## 待你决定 / 未做（都不阻塞）

1. **合同审阅 / 信息监控 / 深度研究可执行**（原交接的语义映射问题的延续）：
   这三个是 trustedEvidence（法律/事实核查/研究，证据是关键）。两条路：
   - 快：给 structural 包 → 立刻能对话，但降级成 advice-only、**丢证据保证**；
   - 慢：实现可信证据投影 + `VerificationRegistry` → 保留证据保证，是较大未来功能。
   一个法律/事实核查专家的安全等级不应擅自降，**需你定方向**。
2. **会话列表 12 条 vs 真正接通的 6 条**：`HaloFixtures.conversations` 是原型假数据，
   与 `productionSingleChatConversations` 不一致。哪些联系人发货是产品决策。
   现状：点不通的会明确显示"该专家未安装"，不再误报存储故障。
3. **Provider 独立启停按钮**：连接测试（只读）已做；启停是真 mutation，需走共享协调器
   + 回滚 + 同 SecretRef 不轮换，留作后续。
4. **P1 群聊真机自然回答的最终确认**：逻辑已通 + 有测试，待手动重配后验一轮。

## 追记（同日晚些）：产品决策落地 + 真机

三项产品决策均已执行：

1. **降级**（`9e2a7a8`）：legal-risk-advisor / fact-checker / industry-researcher 从
   trustedEvidence 降为 structural（advice-only），合同审阅/信息监控/深度研究可对话，
   回复永远带「未核验」。user-researcher 与 finance-tax-analyst 保持证据绑定，
   receipt 机制测试改由 user-researcher 承载，机制本身仍全覆盖。9/9 联系人可打开。
2. **发货**：会话列表 9 条已全部接通（file-processing / translation-expert /
   system-assistant 等纯 fixture 条目仍显示"该专家未安装"）。
3. **真机**：Debug 包从桌面启动会闪退（iOS 限制）；Release 包已装到
   洪智的 iPhone 13 Pro（`00008110-0002504C3663801E`，签名 Automatic + TWZ4C69626）。
   命令：`flutter build ios --release` 然后
   `xcrun devicectl device install app --device <udid> build/ios/iphoneos/Runner.app`。
   注意：同一工程不要并行跑两个 `flutter build`，会互相踩 `.dart_tool`。

另一批 UI/行为（`714d379`，4 个并行子代理完成）：回复中只显示波浪动画（无文字无停止钮）、
输入框无占位文字、对话/市场页去掉右上搜索钮且搜索框可输入过滤、专家团搜索钮弹出
可输入搜索栏、市场分类 Wrap 换行、详情页三按钮不换行 + 技能/工具权限来自真实专家档案、
**保存首个 Key 时自动把全局默认设为目录第一个模型**（临界区内、可回滚、不覆盖已有默认）。

## 真机操作备忘

```bash
# 重装最新构建
cd /Users/cofe/IOS-IM/apps/mobile
flutter build ios --debug --simulator
xcrun simctl install 4569A31A-A611-4ABE-901B-A3B95A330128 build/ios/iphonesimulator/Runner.app
xcrun simctl launch 4569A31A-A611-4ABE-901B-A3B95A330128 com.cofe.haloMobile

# 把 Mac 剪贴板同步进模拟器（复制好 Key 后），再在 app 里点「粘贴」
xcrun simctl pbsync host 4569A31A-A611-4ABE-901B-A3B95A330128
```

注意：`flutter test integration_test/...` 会重装并清掉容器里的 Key/模型配置；
真机验收要在手动持久安装里做，别用 integration test 跑完之后的状态。

## 未提交的本地改动

`apps/mobile/ios/Runner.xcodeproj/project.pbxproj` 加了 `DEVELOPMENT_TEAM = TWZ4C69626`
（真机签名用的个人团队 ID）。开源仓建议不提交，故保留本地未暂存。
