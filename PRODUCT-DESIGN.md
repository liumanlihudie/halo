# IOS-IM 产品与交互设计规格

版本：0.1  
日期：2026-07-28  
状态：待原型实现

## 1. 产品定义

IOS-IM 是一款面向个人用户的 AI 原生即时通讯应用。用户通过类似主流 IM 的低学习成本界面，与不同能力、性格、记忆和模型配置的 AI Agent 建立长期关系并完成实际工作。

它不是企业协同软件，也不是人类社交网络。通讯录里没有真人；朋友圈同样只由 AI Agent 发布内容。用户与 Agent 的主要交互始终发生在对话框中，AI 朋友圈承担信息汇总、成果沉淀和主动推送。

### 1.1 首发用户

- 单个个人用户。
- 同时有工作、资讯和轻度生活需求。
- 已经在多个 AI 产品间切换，希望统一联系人、历史记录和任务成果。

### 1.2 产品价值

- 像找联系人一样找到能力，不需要先选择模型或工作流。
- 让多个 Agent 共享必要背景，同时保留各自私有记忆和性格。
- 把聊天过程转化为文件、结论、任务、监控提醒和朋友圈成果。
- 通过主动消息和 AI 朋友圈，让用户看见 Agent 正在做什么。

### 1.3 非目标

- 不支持真人注册、加好友、真人群聊和真人朋友圈。
- 首发不做企业组织架构、管理员、审批、席位和部门权限。
- 首发不做多 Agent 语音群聊。
- 不把陪伴和暧昧聊天作为产品主轴。
- 不追求完全复刻任何现有 IM 的品牌资产、图标、文案或视觉细节。

## 2. 核心设计原则

1. **对话优先**：命令、追问、文件交换和协作都在对话框完成。
2. **成果可见**：朋友圈只发布有真实来源的任务结果、观察更新或总结。
3. **Agent 是联系人**：模型、工具、提示词和记忆被封装在联系人身份之后。
4. **群聊必须可控**：任何时候用户都能看懂哪些 Agent 会回答以及为什么。
5. **默认开启、可以关闭**：Agent 自动总结朋友圈默认开启，但用户可全局或按 Agent 关闭。
6. **熟悉但有区分**：沿用通用 IM 信息架构，不复制微信商标、图标、绿色品牌色、特有动效和逐像素布局。

## 3. 信息架构

底部固定四个主入口：

1. **对话**
   - 一对一会话
   - 多 Agent 文字群聊
   - 系统通知和任务状态会话
2. **通讯录**
   - 我的 Agent
   - Agent 分组
   - AI 市场
   - Agent 资料
3. **AI 朋友圈**
   - 自动总结
   - 任务成果
   - 资讯更新
   - Agent 协作评论
4. **设置**
   - 个人账户
   - 记忆与隐私
   - 模型与用量
   - 自动总结
   - 通话、通知和安全

## 4. 对话

### 4.1 对话列表

一对一会话和群聊混排，按照最新活动时间排序。

每个列表项至少包含：

- 头像；群聊使用 2×2 Agent 头像拼图。
- 联系人或群名称。
- AI 标识或群成员数量。
- 最新消息；群聊显示“发言 Agent：内容”。
- 时间、未读数量、免打扰或任务进行中状态。
- 不同消息预览：纯文字、图片、文件、语音、任务完成、监控异常、草稿等待确认。

顶部“＋”支持：

- 发起新对话。
- 创建 Agent 群聊。
- 从 AI 市场添加 Agent。
- 扫描或导入文件。

### 4.2 一对一对话

支持：

- 文字消息。
- 拍照、相册、文件、扫描、网页、剪贴板和录音。
- 文件卡片、图片网格、结构化结论、引用来源、进度卡和失败重试。
- 单个 Agent 的豆包端到端双工语音通话。
- 单个 Agent 的 Vidu 视频对话。

消息输入支持普通文本、语音输入、附件和快捷指令。原型不接真实服务，但所有可见操作必须产生页面、状态、弹层或 toast 反馈。

### 4.3 多 Agent 文字群聊

群聊同时包含以下三种发言模式：

#### 自动选择

用户直接发送普通消息时，群聊编排器根据问题和群成员能力选择 1–2 个最合适的 Agent 回答。输入框上方显示“系统将自动选择 Agent”。

#### 指定回答

用户通过 `@Agent` 点名。只有被点名的 Agent 回答；支持一次点名多个 Agent。输入框上方显示即将回答的 Agent。

#### 全员讨论

用户通过 `@所有人` 或点击“让大家讨论”触发。所有 Agent 按顺序发言，可以引用、补充或反驳前面的观点，最后由群主持人生成总结。

### 4.4 群聊状态与控场

- 同一时刻只有一个 Agent 处于“正在输入”。
- 每条消息显示 Agent 头像、名称和其底层模型标签。
- 自动选择时显示“已选择产品经理、研究员”。
- 全员讨论显示阶段：观点收集、交叉讨论、总结。
- 用户随时可以“停止讨论”。
- 一次全员讨论默认每个 Agent 最多一条主要观点，避免刷屏。
- 讨论总结可以保存为文件、置顶到群公告或发布到 AI 朋友圈。

### 4.5 群资料

群资料包括：

- 群头像、群名称和群目标。
- Agent 成员列表与“添加 Agent”。
- 主持 Agent。
- 默认发言模式：自动选择。
- 群共享上下文。
- 允许读取的文件和记忆范围。
- 自动生成讨论总结开关。
- 自动总结到朋友圈开关。
- 清空群聊记录和删除群聊。

### 4.6 首批 mock 群聊

1. **iOS 产品小组**
   - 产品经理、交互设计师、技术架构师、增长顾问。
   - 数据类型：需求讨论、原型链接、任务清单、分歧与总结。
2. **本周信息研判**
   - 信息观察员、研究员、数据分析师。
   - 数据类型：新闻链接、来源可信度、趋势表格、风险提醒。
3. **内容发布团队**
   - 写作顾问、事实核查员、视觉策划。
   - 数据类型：文章草稿、修改意见、配图建议、发布计划。

## 5. 通讯录与 AI 市场

### 5.1 通讯录

通讯录包含工作型、资讯型和生活型 Agent，并提供：

- 搜索。
- 按类别、最近使用、已置顶筛选。
- 在线、忙碌、执行任务、等待确认等状态。
- Agent 详情。
- 发消息、语音通话、视频通话。
- 添加到已有群聊或创建新群。

首批 Agent mock：

- 通用助理。
- 产品经理。
- 研究员。
- 数据分析师。
- 写作顾问。
- 信息观察员。
- 合同审阅助手。
- 翻译专家。
- 日程管家。
- 健身计划师。

### 5.2 AI 市场

市场提供：

- 推荐、效率、研究、内容、生活等分类。
- 搜索 Agent、技能或场景。
- 官方、热门、新上架、免费试用等标识。
- 星级、使用人数、模型、所需权限和预计用量。
- 添加到通讯录。
- 添加后直接加入指定群聊。
- Agent 详情、能力示例、权限说明和试聊。

## 6. AI 朋友圈

### 6.1 定位

朋友圈是信息汇总和成果沉淀页面，不是用户与 Agent 的主要对话入口。用户的指令仍在对话框中完成。

### 6.2 内容类型

- 对话总结：关键结论和后续行动。
- 任务成果：PDF、表格、图片、报告、代码或链接。
- 资讯提醒：重要变化、来源和影响判断。
- 每日/每周总结。
- Agent 工作进度和异常。
- Agent 之间的协作评论。

### 6.3 发布规则

- 默认自动总结并发布，允许全局关闭和按 Agent 关闭。
- 必须存在真实触发源：完成任务、发现变化、收到新数据或达到总结周期。
- 每个 Agent 每日默认最多 1–2 条。
- 原始私密文件不直接公开展示，只显示经过允许的摘要和成果引用。
- 每条内容标注“AI 生成”、来源会话、时间和参与 Agent。
- 用户可以隐藏、删除、收藏、回到原对话或继续追问。

### 6.4 mock 内容

- 三图项目进度。
- PDF 竞品报告。
- 带来源链接的行业更新。
- 数据异常小图表。
- 一周完成事项。
- 失败任务和重试入口。
- 多 Agent 协作评论。
- 仅自己可见的隐私内容。

## 7. 设置

设置页使用多组真实 mock：

- 账户头像、昵称、iCloud 同步状态。
- 记忆与个性化：共享事实记忆、Agent 私有关系记忆、清除记录。
- AI 朋友圈：全局开关、发布频率、隐私范围。
- 模型与用量：本月文本、语音和视频用量；不同模型的调用说明。
- 通话：豆包端到端语音、默认音色、Vidu 视频额度。
- 通知：主动消息、重要提醒、免打扰时段。
- 数据与权限：相册、相机、麦克风、文件。
- 安全：本地锁、Face ID、导出数据、删除账户。
- 关于：生成内容标识、备案与模型信息、用户协议。

## 8. Agent 与模型策略

### 8.1 Agent 配置

每个 Agent 至少包含：

- 身份：名称、头像、简介、类别。
- 模型：提供商、模型标识、能力与成本等级。
- 人设：系统提示词、表达风格、专业边界。
- 工具：搜索、文件、日历、网页、代码等。
- 记忆：共享事实层和私有关系层。
- 通话：语音音色、语音人设、视频形象。
- 主动性：允许主动消息、频率和触发条件。

### 8.2 一对一语音

现有路线为豆包端到端双工语音，不拆成 ASR → 外部 LLM → TTS。

会话启动时根据 Agent 注入：

- `bot_name`
- `system_role`
- `speaking_style`

一条纯 S2S 会话不切换其他大模型。不同 Agent 可以共享豆包端到端模型，通过角色配置表现为不同专家。

### 8.3 文字群聊

文字群聊由应用服务端编排，可以让不同 Agent 使用不同底层模型。编排器负责：

- 判断普通消息、指定回答或全员讨论。
- 选择 Agent 和发言顺序。
- 为每个 Agent构建群共享上下文与私有上下文。
- 保存结构化消息、引用关系和生成状态。
- 控制超时、失败、停止和重试。
- 生成最终总结。

## 9. 核心数据结构

```text
User
  id, name, avatar, preferences

Agent
  id, name, avatar, category, status
  modelProfile, personaProfile, toolIds
  sharedMemoryPolicy, privateMemoryId
  voiceProfile, videoProfile, proactivePolicy

Conversation
  id, type(oneToOne|group|system), title
  memberAgentIds, hostAgentId, defaultReplyMode
  sharedContextPolicy, summaryPolicy

Message
  id, conversationId, senderType(user|agent|system)
  senderId, contentType, content
  replyMode, mentionedAgentIds, replyToMessageId
  status, createdAt, sourceRefs

DiscussionRun
  id, conversationId, triggerMessageId
  selectedAgentIds, phase, currentSpeakerId
  stopRequested, summaryMessageId

Moment
  id, authorAgentId, sourceConversationId
  contentType, title, body, attachments
  participantAgentIds, visibility, createdAt
```

## 10. iOS 技术边界

建议技术路线：

- 客户端：SwiftUI。
- 本地状态：Swift Observation + 可持久化会话缓存。
- 实时消息：WebSocket。
- 文件上传：对象存储预签名上传。
- 一对一语音：豆包端到端实时语音 WebSocket 或 RTC 方案。
- 视频：Vidu 能力按其正式 API 与延迟能力接入；原型阶段仅模拟。
- 服务端：会话、Agent、记忆、消息和群聊编排 API。
- 数据库：关系数据库保存结构化实体，对象存储保存附件，向量或检索层保存长期记忆。

### 10.1 可靠性要求

- 消息采用客户端临时 ID 与服务端正式 ID，支持重试和去重。
- 明确发送中、已发送、失败、Agent 输入中和任务执行中状态。
- 群聊每个生成任务可取消。
- 附件上传失败可单独重试，不重复发送整条消息。
- 历史记录可分页、搜索和恢复。

## 11. 原型范围

HTML 原型模拟一台 iPhone，并提供桌面侧边导航与说明。目标不是连接真实模型，而是验证信息架构、内容密度和主要交互。

必须可点击：

- 四个底部 Tab。
- 一对一会话和三个群聊。
- 新建群聊、选择 Agent、群资料。
- 三种群聊触发方式及生成状态。
- 附件面板、文件卡、成果详情。
- 单 Agent 语音和视频页。
- 通讯录、市场筛选、Agent 详情和添加。
- 朋友圈详情、评论菜单和回到原对话。
- 设置开关、选择项、权限与用量弹层。

## 12. 验收标准

- 首屏能看出这是“只有 AI 联系人的个人 IM”。
- 用户无需阅读说明即可找到群聊，并理解谁会回答。
- 三种群聊模式都有独立、可见的交互结果。
- 语音和视频入口只出现在单 Agent 场景，群聊内不出现通话入口。
- 每个主页面至少包含三种不同内容形态的 mock 数据。
- 所有看起来可点击的元素均提供反馈。
- AI 朋友圈外观接近用户熟悉的朋友圈信息结构，但品牌资产与视觉细节明显区分。
- 原型在桌面展示完整，在窄屏下仍可使用。

## 13. 决策记录

- 2026-07-27：最初设想只做一对一，不做群聊。
- 2026-07-28：确认首发为个人产品，不做企业层面。
- 2026-07-28：确认通讯录和朋友圈均没有真人社交。
- 2026-07-28：确认 AI 朋友圈用于总结与单独发布结果，主要沟通仍在对话框。
- 2026-07-28：确认加入 AI 市场，可添加到通讯录。
- 2026-07-28：确认一对一附件面板包含语音和视频通话。
- 2026-07-28：确认豆包路线是端到端双工，Agent 人设通过会话配置注入，不切换其他大模型。
- 2026-07-28：新增多 Agent 文字群聊，同时保留“不做语音群聊”。
- 2026-07-28：群聊同时包含自动选择、指定 Agent 和全员讨论三种模式。
