# IOS-IM 演示版开发文档

版本：0.2

日期：2026-07-28

状态：可交互演示版已完成

## 1. 文档目的

本文面向产品、设计、iOS、服务端和模型编排开发，说明当前演示版已经验证了什么、正式 MVP 应该开发什么、各阶段如何验收。

产品定义与交互细节见 `PRODUCT-DESIGN.md`，系统设计与接口约定见 `TECHNICAL-DESIGN.md`。

## 2. 产品边界

IOS-IM 是个人 AI 通讯产品，不是企业协作软件，也不包含真人社交。

- 通讯录中只有 AI Agent。
- AI 朋友圈只由 Agent 发布任务成果、信息更新和对话总结。
- 用户与 Agent 的主要沟通发生在对话框。
- 一对一支持文字、图片、文件、拍照、语音和视频。
- 多 Agent 群聊首版只做文字，不做语音群聊。
- 豆包语音采用端到端双工链路，不在同一语音会话中切换其他大模型。
- 首版不做企业组织、管理员、部门、审批和席位。

## 3. 当前演示版

入口：`prototype.html`

### 3.1 主页面

1. 对话：一对一、三个文字群聊、任务和系统状态。
2. 通讯录：Agent 列表、状态、资料和 AI 市场入口。
3. AI 朋友圈：任务成果、资讯、图片、文件和 Agent 协作评论。
4. 设置：个人资料、记忆、模型用量、语音视频、通知、权限与安全。

### 3.2 二级页面

- 单 Agent 对话。
- iOS 产品小组群聊。
- 群资料和共享上下文。
- 创建群聊。
- AI 市场和 Agent 详情。
- Agent 资料。
- 朋友圈成果详情。
- 豆包端到端语音通话。
- Vidu 视频对话。

### 3.3 已实现交互

- 四个主 Tab 与桌面侧栏导航。
- 对话频道包含 12 条可进入会话：
  - 三个独立多 Agent 群聊。
  - 通用助理、数据分析师、日程管家、翻译专家和合同审阅助手。
  - 深度研究任务、文件处理、信息监控和 Halo 系统助手。
- 消息格式覆盖文字、长文、引用、单图、图片网格、PDF/Word/Excel/ZIP、语音转写、网页来源、数据表、趋势图、任务进度、清单、日历、风险和系统通知。
- 异常状态覆盖上传失败、格式不支持、来源读取失败、来源冲突、权限不足、额度不足、服务降级、用户停止和断点续传。
- 一对一语音与视频通话入口统一放在输入框右侧“＋”菜单；顶部导航只保留返回和居中标题。
- 群聊三种模式：
  - 普通消息：自动选择 1–2 个合适 Agent。
  - `@某个 Agent`：仅点名成员回答。
  - `@所有人` / 让大家讨论：顺序发言、补充反驳、阶段状态和总结。
- 停止讨论、保存总结、发布到 AI 朋友圈。
- 群资料、成员、默认规则和共享上下文入口。
- 一对一附件面板、语音通话和视频通话入口。
- 50 位市场专家，覆盖效率、研究、内容、数据、法律财税和生活。
- 市场搜索、分类、每批 10 位增量加载、详情和添加到通讯录。
- 设置资料卡、12 个统一功能图标、开关和模拟弹层。
- 动态安全区、内容滚动和短视口手机缩放。

### 3.4 Mock 数据规则

- 所有 Agent 都有名称、分类、能力描述、模型、标签、成本、评分和权限。
- 50 位市场专家使用唯一 ID，详情页直接绑定所选专家。
- 三个群聊分别覆盖产品讨论、信息研判和内容发布。
- 朋友圈同时覆盖图片、文件、链接、图表、失败任务和周总结。
- 演示版状态只保存在当前页面内，刷新后重置。

## 4. 演示路径

建议按以下顺序演示：

1. 从“对话”依次进入数据分析师、合同审阅助手和文件处理，展示表格图表、风险条款和失败重试。
2. 进入日程管家确认会议，再进入深度研究任务停止生成。
3. 打开三个群聊，比较产品评审、信息研判和内容发布的独立历史。
4. 在“iOS 产品小组”展示输入框上方的当前回答模式。
5. 切换自动选择、`@某个 Agent` 和全员讨论。
6. 启动“让大家讨论”，观察三个阶段并生成总结。
7. 把总结发布到 AI 朋友圈。
8. 进入群资料，查看成员、主持 Agent 和共享上下文。
9. 进入单 Agent 对话，展示附件、语音和视频入口。
10. 进入 AI 市场，搜索“合同”、筛选“法律财税”、滚动加载 50 位专家。
11. 打开专家详情并添加到通讯录。
12. 进入设置，展示身份资料、记忆、用量、权限和安全项。

## 5. 本地运行

在仓库根目录运行：

```bash
python3 -m http.server 4173 --directory IOS-IM
```

访问：

```text
http://127.0.0.1:4173/prototype.html
```

也可直接打开 `prototype.html`，但 HTTP 服务更接近真实浏览器运行环境。

## 6. 测试与验收

### 6.1 原型契约测试

```bash
node --test IOS-IM/prototype.test.cjs
```

测试覆盖：

- 四个主 Tab。
- 三个群聊及三种回复模式。
- 讨论阶段、停止和总结。
- 主页面 mock 数据类型。
- 语音、视频和附件入口。
- 50 位专家、分类、搜索、分页与详情绑定。
- SVG 图标、滚动条、按钮尺寸和设备安全区。
- 非微信品牌边界。

### 6.2 手工验收

- 393 × 852 设备画布内容不被圆角、底栏或输入框遮挡。
- 短桌面视口中完整显示手机边框。
- 所有看起来可点击的控件都有跳转、状态变化、弹层或提示。
- 群聊中没有语音或视频通话入口。
- 一对一中同时存在语音和视频入口。
- 市场默认显示 10 / 50，滚动后可到 50 / 50。
- 设置页所有功能项使用同一图标体系。
- 浏览器控制台无错误。

视觉验收记录和截图见 `design-qa.md`。

## 7. MVP 开发拆分

### 阶段 A：iOS 空壳与本地数据

- SwiftUI 四 Tab、导航和统一设计 Token。
- Conversation、Agent、Message、Moment 本地模型。
- 一对一与群聊静态页面。
- 本地数据库、分页和草稿。

验收：无服务端时可以完整浏览本地样例数据，页面结构与演示版一致。

### 阶段 B：账户、通讯录和文字消息

- Apple 登录或匿名设备账户。
- Agent 通讯录与市场 API。
- WebSocket 消息通道。
- 图片与文件上传。
- 消息发送状态、失败重试、去重和历史分页。

验收：用户可以添加 Agent，完成稳定的一对一文字与附件对话。

### 阶段 C：多 Agent 文字群聊

- 建群、成员和共享上下文。
- 自动选择、指定回答、全员讨论。
- 生成任务取消、超时、重试和群总结。
- 群聊成果发布到 AI 朋友圈。

验收：三种模式行为可预测，用户始终知道谁将回答。

### 阶段 D：语音、视频和主动信息

- 豆包端到端双工语音。
- Agent 角色和音色配置。
- Vidu 视频会话。
- 主动消息、任务状态与 AI 朋友圈自动总结。

验收：语音链路可打断、可挂断、可恢复；朋友圈内容可追溯到来源任务或对话。

### 阶段 E：隐私、计费和上架

- 权限最小化、数据导出和账户删除。
- 模型与语音视频用量统计。
- 内容标识、隐私政策、用户协议和模型备案信息。
- TestFlight、崩溃监控和灰度开关。

## 8. 演示版与正式产品差异

| 能力 | 演示版 | 正式 MVP |
|---|---|---|
| 数据 | HTML 内置 Mock | 服务端 + 本地缓存 |
| 登录 | 密码、验证码、Apple、忘记/修改密码与账号切换 Mock | Apple 登录、服务端会话与真实验证码 |
| 文字模型 | 定时模拟 | 多模型网关流式返回 |
| 群聊编排 | 前端固定脚本 | 服务端状态机 |
| 文件 | 静态卡片 | 预签名上传与解析 |
| 语音 | 页面演示 | 豆包端到端双工 |
| 视频 | 页面演示 | Vidu 正式 API |
| 记忆 | 展示字段 | 共享事实层 + Agent 私有层 |
| 朋友圈 | 静态与本地追加 | 可追溯成果流 |
| 持久化 | 刷新重置 | 本地数据库 + 云端同步 |

## 9. 上线前必须补齐

- 豆包和 Vidu 的正式接口、计费、并发和内容安全评估。
- Apple 隐私清单、权限文案和账户删除闭环。
- 消息加密、敏感日志脱敏和附件生命周期。
- Agent 提示词版本、工具授权和越权防护。
- 群聊成本上限、最大轮数和熔断策略。
- AI 生成标识、来源引用和错误免责声明。
- 弱网、后台恢复、推送、断线重连和数据迁移测试。

## 10. 账户、认证与 Token 演示路径

设置页已经补齐个人产品的常规账户闭环：

1. 「账户中心」查看身份资料、登录方式、设备、修改密码和切换账号。
2. 「Token 余额」查看余额、用量估算、套餐和最近交易。
3. 充值流程可选择 50K、120K 或 260K 套餐，以及 Apple Pay、支付宝或微信支付的模拟方式。
4. 支付页提供成功与失败两种演示结果；成功后余额和交易记录同步更新。
5. 「切换账号」包含当前账号、产品研究和个人生活三个独立 mock 账号。
6. 「退出登录」必须经过确认，确认后进入无主标签栏的登录页。
7. 登录页支持密码登录、验证码登录和 Apple 登录模拟。
8. 「忘记密码」包含联系方式、六位验证码和设置新密码三步。
9. 「修改密码」包含当前密码、新密码、确认密码及字段级错误反馈。

演示验证码为 `246810`。原型只检查六位输入是否完整，不发送真实短信或邮件。
